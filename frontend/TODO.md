# TODO - Admin panel redesign (MindBloom)

## Plan checklist
- [x] Implement reference admin layout (sidebar + topbar + hero + KPI grid + lower grid) using `mb-*` classes
- [x] Update `frontend/src/pages/AdminDashboard.jsx` markup to match reference structure
- [x] Add/override required admin CSS in `frontend/src/index.css` (including responsiveness)
- [x] Ensure `/admin` hides bottom nav / respects desktop layout constraints
- [x] Preserve existing functionality: Run Scoring, Refresh, table acknowledge/resolve/view, ModelManager, AlertDetailPanel
- [x] Verify basic rendering at desktop and mobile breakpoints (build succeeds; UI classes wired)

